# Local scratch / exported-chat artifacts (from the "Download chat as XML" feature
# and its HTML predecessor) — these are personal exports, not source.
claude-test-chat-*.html
*_[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].xml

# Packaged build
claudeplusplus.zip

# A local copy of claude.ai's icon font, handy for looking up what a private-use
# codepoint actually draws (see CPP.util.ICON). We never ship a font — our glyphs
# render through claude's own document-wide @font-face — so it isn't source.
anthropicons.woff2
